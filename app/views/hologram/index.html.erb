
<!DOCTYPE html>
<style>
  .holos-container {
    width: 100%;
    text-align: left;
  }

  .holo {
    display: inline-block;
    width: 250px;
    border: 1px solid #ccc;
    margin: 10px;
    padding: 10px;
    vertical-align: top;
    text-align: center;
    background-color: white;
    height: 350px;
  }

  .actor {
    font-weight: bold;
    margin-bottom: 5px;
  }

  .image img {
    max-width: 200px;
    max-height: 200px;
    vertical-align: center;

  }

  .price {
    color: green;
    font-weight: bold;
  }

  body {
  background-color: black;
  }


</style>

<div class="holos-container">
  <% @holograms.each do |holo| %>
    <div class = "holo">
      <div class = "actor"><%= holo.actor %></div>
      <div class = "image"><%= image_tag(holo.image) %></div>
      <div class = "price">$<%= holo.price %></div>
      <div class = "view-button"><%= link_to "View Item", hologram_path(holo)%></div>
    </div>
  <% end %>
</div>
